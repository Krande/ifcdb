name: ifcdb-dev
channels:
  - ifcopenshell
  - conda-forge
dependencies:
  - ifcopenshell
  - python==3.10.5
  - pyparsing
  - pytest
  - toposort
  - httpx
  - fastapi
  - uvicorn
  - starlette
  - pip
  - pip:
      - edgedb
      - rdflib
      - fastapi-azure-auth
      - azure-storage-queue
      - azure-identity
      - pydantic[dotenv]
      - fake-bpy-module-3.2
      - anyio[trio]